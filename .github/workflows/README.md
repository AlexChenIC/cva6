# OpenHW CI (AWS)

This directory contains OpenHW-specific CI helpers and scripts for running
CVA6 regressions on a self-hosted AWS GitHub Actions runner.

The initial focus is Verilator-based regression on AWS. Additional simulator
flows (e.g. Questa) may be added later, using the same structure.
