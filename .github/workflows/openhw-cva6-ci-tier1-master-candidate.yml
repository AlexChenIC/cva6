# Copyright 2026 OpenHW Group
# Licensed under the Apache License, Version 2.0, see LICENSE for details.
# SPDX-License-Identifier: Apache-2.0
#
# Fast public coverage using the same cook.py target and testlist contract as
# the Thales matrix. The public backend remains Verilator TestHarness + Spike;
# Thales uses VCS/UVM. Zcmt is deferred until the public runner has a compatible
# LLVM 20 or GCC 15 toolchain, so the compiler ISA is recorded explicitly.

name: openhw-cva6-ci-tier1-master-candidate

on:
  push:
    branches:
      - master_candidate
  pull_request:
    branches:
      - master_candidate
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: openhw-cva6-ci-tier1-master-candidate-${{ github.ref }}
  cancel-in-progress: true

env:
  SPIKE_TANDEM: 1
  NUM_JOBS: 8
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  setup-tools:
    name: Setup Tools
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup CVA6 environment
        uses: ./.github/actions/setup-cva6-env
        with:
          install-tools: "true"

  execute-rv32-tier1:
    name: RV32 Tier1 ${{ matrix.config }} / ${{ matrix.testcase }}
    runs-on: ubuntu-latest
    needs: setup-tools
    strategy:
      fail-fast: false
      matrix:
        include:
          - config: cv32a60x_axi
            testcase: base-rv32-p-smoke
            mode: cook-testlist
            simulator: veri-testharness,spike
            toolchain: github_actions_gcc
            install_script: install-riscv-tests
            testlist: verif/tests/base_rv32_p.yaml
            test_name: rv32ui-p-add
            compiler_march: rv32imc_zba_zbb_zbs_zbc_zicsr
            compiler_march_reason: public-gcc-13.2-does-not-support-zcmt
            expected_hier: axi

          - config: cv32a65x_axi
            testcase: base-rv32-p-smoke
            mode: cook-testlist
            simulator: veri-testharness,spike
            toolchain: github_actions_gcc
            install_script: install-riscv-tests
            testlist: verif/tests/base_rv32_p.yaml
            test_name: rv32ui-p-add
            compiler_march: rv32imc_zba_zbb_zbs_zbc_zicsr
            compiler_march_reason: public-gcc-13.2-does-not-support-zcmt
            expected_hier: axi

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup CVA6 environment
        uses: ./.github/actions/setup-cva6-env
        with:
          install-cook-dependencies: "true"

      - name: Install prerequisites
        run: source ci/install-prereq.sh

      - name: Run Tier 1
        env:
          TIER_NAME: Tier 1 master_candidate
          TIER_MODE: ${{ matrix.mode }}
          TIER_CONFIG: ${{ matrix.config }}
          TIER_TESTCASE: ${{ matrix.testcase }}
          TIER_SIMULATOR: ${{ matrix.simulator }}
          TIER_TOOLCHAIN: ${{ matrix.toolchain }}
          TIER_INSTALL_SCRIPT: ${{ matrix.install_script }}
          TIER_TESTLIST: ${{ matrix.testlist }}
          TIER_TEST_NAME: ${{ matrix.test_name }}
          TIER_COMPILER_MARCH: ${{ matrix.compiler_march }}
          TIER_COMPILER_MARCH_REASON: ${{ matrix.compiler_march_reason }}
          TIER_EXPECTED_HIER: ${{ matrix.expected_hier }}
          TIER_EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          TIER_EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha || '' }}
        run: bash .github/scripts/run-tier-regression.sh

      - name: Write job summary
        if: always()
        run: |
          {
            echo "## RV32 Tier 1"
            echo '```text'
            cat ci-results/metadata.txt 2>/dev/null || true
            cat ci-results/failure_summary.log 2>/dev/null || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tier1-master-candidate-${{ matrix.config }}-${{ matrix.testcase }}
          path: |
            ci-results/
            artifacts/reports/
            build/${{ matrix.config }}/compile/
            build/${{ matrix.config }}/simulation/sim_verilator_testharness/
          if-no-files-found: warn
          retention-days: 14
